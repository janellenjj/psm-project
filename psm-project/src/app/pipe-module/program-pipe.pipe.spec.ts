import { ProgramPipePipe } from './program-pipe.pipe';

describe('ProgramPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramPipePipe();
    expect(pipe).toBeTruthy();
  });
});
